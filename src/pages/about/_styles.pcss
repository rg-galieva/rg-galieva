@import "../../assets/styles/vars.gcss";

.cont {
  position: relative;
  z-index: var(--z-cont);
}

.desc {
  max-width: 80%;

  & p {
    margin-bottom: .8rem;
  }
}

@media (max-width: 800px) {
  .desc {
    max-width: 95%;
  }
}